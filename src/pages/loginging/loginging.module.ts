import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { LogingingPage } from './loginging';

@NgModule({
  declarations: [
    LogingingPage,
  ],
  imports: [
    IonicPageModule.forChild(LogingingPage),
  ],
})
export class LogingingPageModule {}
